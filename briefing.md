# 학습 결과물 정리표

| 항목 | 내용 |
|---|---|
| 제작자 | 품질경영팀 허재영 프로 |
| 명칭 | 화학물질 변경이력 조회 |
| URL | https://chemical-change-history-demo.vercel.app (Vercel 배포, 확인됨) — 로컬 실행도 가능(`index.html` 또는 `1페이지_메인대시보드.html`) |
| 주요기능 | 1) 변경이력 조회 메인 대시보드 — CAS No./재료/제품 3종 DB의 일일·월간·연간 변경 현황, 최근 10건 표, 월별 누적 히스토그램(완성) <br>2) CAS No. DB 조회 — 하위 메뉴 2개로 구성: "CAS No. 변경 이력"(조건별 검색 및 결과 표, 1차 구현 완료), "CAS No. 상세 정보"(화면 구조만 예시로 구현 — 산업안전보건법/화학물질관리법/화학물질 등록평가법/해외규제/물질정보/유해위험성분류 6개 섹션, 실제 데이터는 아직 없고 Mock CAS No. 64-17-5에만 placeholder 예시 값 연결됨) <br>3) (미완성) 재료 DB 조회(3페이지), 제품 DB 조회(4페이지) — 메뉴만 존재, 클릭 시 "추후 구축 예정" 안내만 표시 |
| 사용대상 | 연구 및 품질부문 |
| 비고 | 아래 참고 |

## 비고 상세

**제작/개발**
- 제작에 사용한 AI 도구: Claude Code (Claude Sonnet 5) — git 커밋에 "Co-Authored-By: Claude Sonnet 5" 기록
- 개발 기술: HTML/CSS/JavaScript(바닐라), 외부 프레임워크·차트 라이브러리 미사용. 히스토그램은 외부 라이브러리(Chart.js) 대신 순수 SVG로 직접 구현(사내망 등 오프라인 환경에서도 렌더링되도록 하기 위함)
- 폰트: Pretendard(오픈소스, OFL 라이선스) 적용. CDN 대신 폰트 파일을 `fonts/` 폴더에 내려받아 저장소에 포함(자체 호스팅) — 오프라인/사내망에서도 동일하게 보이도록 하기 위함, 라이선스 원문은 `fonts/LICENSE.txt`에 포함
- 데이터 저장 방식: 별도 DB/서버 없음. 화면에 보이는 데이터는 JS 코드 안에 하드코딩된 Mock(예시) 데이터
- 접근 차단: `index.html`에 PIN 입력 게이트 추가(현재 PIN: 1946, 4자리). 입력창 자체는 최대 10자리까지 입력 가능하도록 여유를 두었으나 실제 비교 대상은 여전히 4자리 PIN. PIN 원문은 코드에 저장하지 않고 SHA-256 해시로만 비교, 인증 성공 시 `localStorage`에 플래그를 저장해 새로고침·새 탭에서도 재입력 없이 통과됨(최초에는 세션 단위인 `sessionStorage`로 구현했으나, 새로고침 시 다시 뜨는 문제로 `localStorage`로 변경 — 브라우저 저장공간을 지우기 전까지는 계속 통과됨)
- SHA-256 계산은 브라우저 내장 Web Crypto API 대신 **순수 JS로 직접 구현**한 함수를 사용함 — Web Crypto는 HTTPS/localhost가 아닌 사내망 IP(`http://192.168.x.x` 등)에서는 "보안 컨텍스트"가 아니라서 막히는 문제가 있어(로컬 Node 서버로 테스트 중 실제 발생), 어떤 접속 방식에서도 PIN 검증이 동작하도록 수정함
- 로컬 네트워크 공유: `localhost_실습/server.js`(Node 기본 http 모듈)가 이 프로젝트 폴더를 그대로 서빙하도록 설정되어 있어, 같은 네트워크의 다른 사람도 접속해 PIN 게이트부터 동일하게 볼 수 있음. 포트는 `PORT` 환경변수로 지정 가능(기본값 3000) — 같은 PC에서 별도로 만든 Next.js 앱(`chemical-change-history-app`)이 3000번을 쓰는 경우 이 서버는 `PORT=3001 node server.js`처럼 다른 포트로 띄움. 이 서버는 PC가 켜져 있고 해당 프로세스가 살아있는 동안만 유효(터미널 종료/PC 종료 시 함께 꺼짐) — 항상 접속 가능해야 하면 Vercel 배포 주소 사용 권장
- 잘못 인코딩된 요청(예: URL에 깨진 문자) 하나로 서버 프로세스 전체가 죽는 버그가 있어 수정함(`decodeURIComponent` 실패 시 400만 응답하고 서버는 계속 살아있도록)

**실행 방법**
- 실행: `index.html` 또는 `1페이지_메인대시보드.html`을 브라우저로 열면 바로 동작(별도 설치·서버 불필요)
- 필요 파일: `common.css`, `common.js`, `fonts/PretendardVariable.woff2`가 같은 폴더 구조로 있어야 함. `logo.png`, `user-photo.png`, `icon-daily/monthly/yearly.png`는 있으면 자동 적용, 없어도 기본값(이니셜/텍스트)으로 대체되어 정상 동작
- 인터넷/외부 서비스 연결: 불필요. CDN·외부 API 호출 없이 로컬 파일만으로 완전히 동작하도록 설계됨

**서비스 자체의 AI 사용 여부**
- 없음. 화면에 표시되는 로직은 고정 Mock 데이터 집계·표시일 뿐, 런타임에 AI를 호출하는 기능은 없음 (제작 과정에 AI 도구를 쓴 것과는 별개)

**남은 문제 / 다음 작업**
- 3페이지(재료 DB 조회), 4페이지(제품 DB 조회) 미구현 — 현재는 안내 알림만 표시
- 현재 Mock 데이터 → 실제 DB/사내 시스템 연동 필요
- 헤더의 소속팀/이름/사진이 하드코딩 상태 — 로그인 계정 연동 필요
- PRD에 기록된 확인 필요 사항 2건: (1) 제품 요약 표의 1·2번째 컬럼명이 모두 "제품명"으로 표기되어 제품코드/제품명 구분 필요, (2) "월간현황" 카운팅 기준을 연도+월로 볼지 월만으로 볼지 확인 필요(현재는 연도+월 기준으로 구현)
- 코칭 시 확인할 질문: 실제 배포 방식(GitHub Pages vs Vercel 중 확정), 실 데이터 연동 계획/일정, 로그인·접근권한 적용 여부
- "CAS No. 상세 정보" 화면은 큰 틀(섹션 구성)만 예시로 잡아둔 상태 — 실제 CAS No.별 데이터(법령 준수 현황, 유해성 정보 등)를 어떻게 입력/연동할지는 아직 미정(CSV 입력 기능과 연계될 예정, 별도 논의 중)

**보안/공유 시 확인할 사항**
- 저장소(git)에는 실제 프로필 사진·실명이 올라가지 않도록 `.gitignore`에 `user-photo.png`, `logo.png`, `icon-*.png`, `.vercel`이 등록되어 있고, 화면 표시용 이름도 예시 플레이스홀더("홍길동 프로")로 대체되어 있음(PRD 8.1 기록)
- 다만 로컬 작업 폴더에는 실제 파일(`user-photo.png` 등)이 남아있을 수 있으므로, 화면 녹화/캡처를 공유할 경우 로컬 실행 화면인지 배포본 화면인지 확인 필요
- 현재 데이터는 모두 Mock(예시)이라 실제 업무상 민감정보는 포함되어 있지 않음. 다만 이후 실 데이터 연동 시점부터는 별도 보안 검토 필요
- 배포 주소가 외부에 공개된 상태인지(GitHub Pages/Vercel 등 퍼블릭 여부)는 확인 필요 — 공유 승인 여부는 본 문서에서 판단하지 않음
- `index.html`의 PIN 게이트는 백엔드가 없는 정적 페이지의 한계상 완전한 보안이 아님 — 페이지 소스에 PIN의 SHA-256 해시가 노출되어 있어 오프라인으로 4자리(0000~9999) 전수 대입이 가능하고, 각 페이지(`1페이지_메인대시보드.html` 등) 파일명을 직접 알면 PIN 게이트를 거치지 않고 바로 접근 가능함. 가벼운 접근 차단 용도로만 사용 권장
