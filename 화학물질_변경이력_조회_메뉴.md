# 화학물질 변경이력 조회 메뉴 - 기획 문서

## 문서 정보
- 문서명: 화학물질 변경이력 조회 메뉴 PRD (본 md 파일 = PRD)
- 최종 수정일: 2026-09-11
- 상태: 1페이지(메인 대시보드) 완성 / 2페이지(CAS No. DB 조회) 1차 구현 완료 / 3~4페이지 추후 작성 예정

## 목차
1. 개요
2. 화면 사용자
3. 공통 UI 요소 (전 페이지 공통)
   3.1 공통 헤더
   3.2 좌측 카테고리 메뉴 (네비게이션)
4. 1페이지 - 변경이력 조회 (메인 대시보드)
   4.1 상단 요약 통계
   4.2 최근 변경이력 요약 표
   4.3 월 누적 변경이력 히스토그램
5. 2페이지 - CAS No. DB 조회
   5.1 조회(검색) 영역
   5.2 조회 결과 표
6. 3 / 4페이지 (추후 작성 예정)
7. 세부 조회 화면 (공통 규칙)
8. 파일 구조
9. 확인 필요 사항 (Open Issues)
10. 수정 이력

---

## 1. 개요
- 목적: 화학물질(CAS No.) / 재료 / 제품 DB의 변경이력을 한눈에 조회할 수 있는 메뉴 신규 구축
- 향후 지속적으로 기능/화면이 업그레이드될 예정이므로 확장 가능한 구조로 설계

## 2. 화면 사용자
- 품질팀
- 연구팀

## 3. 공통 UI 요소 (전 페이지 공통)

### 3.1 공통 헤더
- 좌측 상단: SP SAMHWA 로고
  - 현재는 로고 이미지 없음 → 추후 로고 파일 업로드 시 교체 가능한 구조로 구현
  - 디자인(7차 수정): 로고 영역(이미지/대체 텍스트 배지) 크기 확대 (높이 36px→44px, 텍스트 15px→20px)
- 상단 대제목: **"화학물질 변경이력 조회"**
- 우측 상단: 실시간 시간/일자 표시 (1초 단위 갱신)
- 우측 상단, 시계 **바로 왼쪽**에 소속팀 + 이름 표시
  - 현재는 하드코딩: **"품질경영팀 허재영 프로"**
  - 추후 로그인/DB 연동 시 접속 계정 기준으로 자동 표시되도록 전환 예정
  - 디자인(3차 수정): 텍스트만 나열하던 방식이 밋밋하여 → 이니셜 아바타(원형, 그라데이션) + 팀명/이름 2단 표시 + 배지(pill) 형태로 고급스럽게 수정
  - 프로필 사진(4차 수정): 아바타 원 안의 이니셜 텍스트 대신 **실제 프로필 사진**(`user-photo.png`)을 표시 → 사진 파일이 없을 경우 이니셜로 자동 대체
  - 추후 API/DB 연동 시 사진·소속팀·이름이 로그인 계정 기준으로 모두 자동 연동될 예정
- 전반적으로 깔끔하고 세련된 UI 지향

### 3.2 좌측 카테고리 메뉴 (네비게이션) — 신규 (5차 수정: 2026-09-11)
- 화면 좌측에 고정된 카테고리 메뉴(사이드바) 신규 추가, 전 페이지 공통
- **1페이지가 최초 진입 화면(첫 화면)**
- 메뉴 구성

| 순번 | 메뉴명 | 대상 페이지 |
|---|---|---|
| 1 | 변경이력 조회 | 1페이지 (메인 대시보드) |
| 2 | CAS No. DB 조회 | 2페이지 |
| 3 | 재료 DB 조회 | 3페이지 (추후 작성) |
| 4 | 제품 DB 조회 | 4페이지 (추후 작성) |

- 현재 위치한 페이지의 메뉴 항목은 강조 표시(활성 색상 + 좌측 색상 바)
- 디자인(6차 수정): 네이비 그라데이션 배경 + 메뉴별 아이콘 적용
  - 메뉴 크기 확대(패딩·폰트 상향) 및 글자색을 **흰색 계열**로 변경하여 가시성 개선

## 4. 1페이지 - 변경이력 조회 (메인 대시보드)
- 상태: **완성**

### 4.1 상단 요약 통계
- 기존이력 대비 변경사항 발생 현황을 한눈에 보여주는 대시보드
- 구분 기준: CAS No. DB / 재료 DB / 제품 DB (3종, 컬럼별 1개씩)
- 일일/월간/연간 현황을 카드로 분리하지 않고, 한 줄(행)에 3개 수치가 모두 보이도록 통합 표시
- 아이콘: 사용자가 3개(일일/월간/연간 각각용) 아이콘 파일을 제공 예정 → 파일 업로드 시 자동 교체되는 구조로 구현 (로고와 동일한 방식)

**카운팅 규칙 (변경일자 기준, 아래 변경이력 표의 데이터로 산출)**
- 일일현황: 변경이력의 **변경일자**가 페이지 우측 상단에 표시된 **오늘 날짜**와 동일한 건수
- 월간현황: 변경이력의 **변경일자**가 우측 상단 날짜와 **연도·월**이 동일한 건수
- 연간현황: 변경이력의 **변경일자**가 우측 상단 날짜와 **연도**가 동일한 건수
- ※ 월간현황은 "월"만 언급되었으나, 연도가 달라도 월만 같으면 합산되는 것을 방지하기 위해 연도까지 함께 비교하는 것으로 구현함 → 의도와 다르면 알려주세요 (Open Issue에도 기록)

### 4.2 최근 변경이력 요약 표
- 각 구분(CAS No. DB / 재료 DB / 제품 DB)별로 표 1개씩, 총 3개 표
- 전체 이력이 아닌 **최근 10건만** 우선 표시
- 표 우측 상단에 **더보기 버튼** 배치 → CAS No.는 2페이지로 연동 이동(구현 완료), 재료/제품은 3·4페이지 완성 후 연동 예정(현재는 안내 알림)
- 표 컬럼 구성 (공통 구조, 4개 컬럼)

| 열 | CAS No. 표 | 재료 표 | 제품 표 |
|---|---|---|---|
| 1 | CAS NO. | 재료코드 | 제품명 |
| 2 | 화학물질명 | 재료규격 | 제품명 |
| 3 | 등록일자 (공통) | 등록일자 (공통) | 등록일자 (공통) |
| 4 | 변경일자 (공통) | 변경일자 (공통) | 변경일자 (공통) |

> ※ 제품 표의 1,2번째 컬럼이 모두 "제품명"으로 언급됨 → 확인 필요 (9번 항목 참고)

### 4.3 월 누적 변경이력 히스토그램
- CAS No. / 재료 / 제품 구분별로 각 1개씩, 총 **3개 히스토그램**
- 표현 방식: 월별 수치 표 + 막대 그래프 + 꺾은선 그래프 (막대·꺾은선 결합형 콤보 차트)
- 구현 방식: 외부 차트 라이브러리(Chart.js, CDN) 사용 시 사내망/오프라인 환경에서 로딩이 안 돼 그래프가 안 보이는 문제 발생 → **외부 라이브러리 없이 순수 SVG로 직접 그리는 방식**으로 구현 (네트워크 연결 여부와 무관하게 항상 표시됨)
- **데이터 연동**: 그래프용 수치를 별도 관리하지 않고 **중단의 변경이력(요약 표)과 동일한 데이터를 기준으로 월별 집계**하여 상단 현황·중단 이력·하단 그래프 3곳이 모두 같은 원본 데이터로 연동됨
- **그래프 틀(축) 표시**: 플롯 영역 테두리 + 좌측 y축 눈금(수치) 및 가로 눈금선 표시
- **값 라벨 표시**: 각 월 막대/꺾은선 지점 바로 위에 해당 월의 건수를 숫자로 표시 (0건인 달은 라벨 생략)
- **명칭**: 히스토그램 제목을 구분별로 구분 표기 — "월 누적 CAS No. 변경이력" / "월 누적 재료 변경이력" / "월 누적 제품 변경이력"
- **X축 기간 고정**: "오늘 기준 최근 12개월 롤링"이 아닌 **올해 1월~12월로 고정** 표시
  - 아직 도래하지 않은 미래 월(예: 9월인 현재 시점의 10·11·12월)은 막대·꺾은선·수치 라벨을 그리지 않고, 월 이름만 흐리게 표시하여 "데이터 없음(미도래)" 상태를 시각적으로 구분

### 1페이지 레이아웃 요약
1. 상단: 요약 통계 9칸 (일일/월간/연간 × CAS No./재료/제품)
2. 중단: 최근 10건 요약 표 3개 (CAS No./재료/제품, 각 더보기 버튼)
3. 하단: 월 누적 변경이력 히스토그램 3개 (CAS No./재료/제품)

## 5. 2페이지 - CAS No. DB 조회 (신규: 2026-09-11)
- 상태: **1차 구현 완료**
- 목적: CAS No. DB 전체를 조건별로 검색·조회

### 5.1 조회(검색) 영역
- 입력 항목 4개 + 조회 버튼(맨 우측)

| 항목 | 입력 방식 | 규칙 |
|---|---|---|
| CAS NO. | 텍스트 입력 | 숫자와 하이픈(-) 조합만 허용, 입력 중 공백이 들어가면 자동으로 제거하여 이어붙임 |
| 등록일자 | 달력 선택 또는 직접 숫자 입력 | `<input type="date">`로 구현 (달력 클릭 또는 키보드 숫자 입력 모두 가능) |
| 변경일자 | 달력 선택 또는 직접 숫자 입력 | 등록일자와 동일 방식 |
| 조회 구분 | 드롭다운 | 전체 / 동일 / 변경 (아래 5.1.1 규칙 참고) |

#### 5.1.1 조회 구분 드롭다운 규칙
- **전체**: 별도 필터 규칙 없이 전체 CAS NO. 출력, 정렬은 **최신 등록일자가 상단**
- **동일**: 변경이력이 없는 경우(변경일자 공란)인 CAS NO.만 전체 출력, 정렬은 **최신 등록일자가 상단**
- **변경**: 변경일자가 존재하는(값이 있는) CAS NO. 기준으로 출력, 정렬은 **최신 변경일자가 상단**
- CAS NO./등록일자/변경일자 입력값이 있으면 위 규칙으로 추려진 목록에 추가로 조건 필터링(AND 조건)

### 5.2 조회 결과 표
- 컬럼: **CAS NO. / 기존 / 등록일자 / 변경 / 변경일자 / 상세조회** (6차 수정: 상세조회 컬럼 추가)
- "기존"과 "변경" 컬럼은 해당 레코드가 어느 쪽에 해당하는지에 따라 **Y로 표기**
  - 변경일자가 공란(= 한 번도 변경된 적 없음) → **기존 = Y**, 변경 = 공란
  - 변경일자가 존재(= 변경 이력 있음) → **변경 = Y**, 기존 = 공란, 변경일자 셀은 적색 강조(7번 공통 규칙과 동일)
- **상세조회(6차 수정)**: 행마다 상세조회 버튼(회색 배경, 검정 글자) 배치 → 클릭 시 해당 CAS No.의 상세 이력 페이지로 연동 예정(현재는 준비중 안내)
- 결과 건수를 표 상단에 함께 표시
- 표 전체 글자 크기를 가독성 좋게 확대(6차 수정)

## 6. 3 / 4페이지 (추후 작성 예정)
- 3페이지 - 재료 DB 조회: (미정, 2페이지와 유사한 구조로 예상)
- 4페이지 - 제품 DB 조회: (미정, 2페이지와 유사한 구조로 예상)

## 7. 세부 조회 화면 (공통 규칙)
- 변경사항을 발생시킨 컬럼(구분인자)을 적색으로 표기
- 모든 표의 상단 컬럼 헤더(th)는 **좌측 카테고리 메뉴와 동일한 배경색(네이비 그라데이션) + 동일한 글자색(흰색 계열)**으로 통일 (7차 수정, 전 페이지 공통 적용)

## 8. 파일 구조
- `index.html` : GitHub Pages 접속 시 1페이지로 자동 이동하는 진입점
- `common.css` : 헤더, 좌측 네비게이션, 카드/표 등 전 페이지 공통 스타일
- `common.js` : 시계, 날짜 유틸, 아이콘 자동교체, CAS NO. 입력 규칙 등 공통 스크립트
- `1페이지_메인대시보드.html` : 변경이력 조회 (메인 대시보드)
- `2페이지_CASNo_DB조회.html` : CAS No. DB 조회
- `3페이지_재료_DB조회.html` : 재료 DB 조회 (추후 작성)
- `4페이지_제품_DB조회.html` : 제품 DB 조회 (추후 작성)
- `logo.png`, `user-photo.png`, `icon-daily.png`, `icon-monthly.png`, `icon-yearly.png` : 교체용 이미지 (있으면 자동 반영, 없으면 기본값 표시)
  - `.gitignore`에 등록되어 **git/GitHub에는 올라가지 않음** (실사진·실명 등 개인정보 성격 자산이라 로컬에서만 사용, 공개 배포본에서는 자동으로 기본값(이니셜/로고 텍스트)으로 대체됨)

### 8.1 GitHub Pages 공개 배포 관련 (8차 수정: 2026-09-11)
- GitHub Pages(무료 플랜)는 공개(public) 저장소에서만 동작 → 배포본은 개인 식별 정보 노출을 방지하기 위해 다음과 같이 처리
  - 실제 프로필 사진(`user-photo.png`) 및 실명("허재영")은 저장소에 포함하지 않음
  - 화면에 표시되는 사용자명은 예시용 플레이스홀더 **"홍길동 프로"**로 대체 (로컬 원본 파일/문서와 다를 수 있음)
  - 소속팀명("품질경영팀")은 개인 식별 정보가 아니므로 그대로 유지

## 9. 확인 필요 사항 (Open Issues)
- 제품 요약 표의 1,2번째 컬럼이 둘 다 "제품명"으로 언급됨 → 제품코드/제품명 구분 여부 확인 필요
- 월간현황 카운팅 시 "월"만 비교할지, "연도+월"을 함께 비교할지 확인 필요 (현재는 연도+월로 구현)

## 10. 수정 이력
- 2026-09-11 (1차): 상단 요약을 카드 9개(3×3 분리형)에서 컬럼별 1행 3열 통합형으로 변경, 아이콘 사용자 제공 예정으로 변경, 일일/월간/연간 카운팅 규칙 정의, 히스토그램을 Chart.js(CDN) → 순수 SVG 렌더링으로 변경
- 2026-09-11 (2차): 히스토그램에 틀/좌측 눈금 추가, 막대 위 수치 라벨 표시, 히스토그램 데이터를 중단 변경이력과 동일 원본으로 연동, 헤더 시계 좌측에 소속팀+이름("품질경영팀 허재영 프로", 하드코딩 → 추후 로그인 연동) 표시 추가
- 2026-09-11 (3차): 헤더 소속팀+이름 표시를 이니셜 아바타 + 배지형 디자인으로 고급화, 히스토그램 제목을 "월 누적 CAS No./재료/제품 변경이력"로 구분 표기
- 2026-09-11 (4차): 헤더 아바타를 이니셜 텍스트 → 실제 프로필 사진(user-photo.png)으로 교체(추후 API/DB로 사진·팀·이름 자동 연동 예정), 월 누적 히스토그램 X축을 "최근 12개월 롤링"에서 "올해 1~12월 고정"으로 변경하고 아직 도래하지 않은 미래 월은 그래프를 그리지 않도록 수정
- 2026-09-11 (5차): 좌측 카테고리 메뉴(네비게이션) 신규 추가 및 전 페이지 공통 요소를 `common.css`/`common.js`로 분리, 2페이지(CAS No. DB 조회) 신규 구현 — 조회 영역(CAS NO./등록일자/변경일자/조회구분 드롭다운/조회버튼) 및 결과 표(CAS NO./기존/등록일자/변경/변경일자) 구현
- 2026-09-11 (6차): 좌측 카테고리 메뉴 크기 확대 + 글자색을 흰색 계열로 변경(가시성 개선), 2페이지 결과 표 글자 크기 확대, 결과 표에 "상세조회" 컬럼과 행별 상세조회 버튼(회색 배경/검정 글자) 추가
- 2026-09-11 (7차): 좌측 상단 SP SAMHWA 로고 크기 확대, 전 페이지 표의 상단 컬럼 헤더 배경/글자색을 좌측 카테고리 메뉴와 동일하게 통일
- 2026-09-11 (8차): GitHub Pages 공개 배포를 위해 실제 프로필 사진/실명을 플레이스홀더("홍길동 프로")로 교체, 이미지 자산을 .gitignore로 제외, 루트 진입용 index.html 추가
